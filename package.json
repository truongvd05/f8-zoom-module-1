{
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "deploy": "vite build && gh-pages -d dist"
    },
    "dependencies": {
        "ejs": "^3.1.10",
        "gh-pages": "^6.3.0",
        "vite": "^6.3.5",
        "vite-plugin-ejs": "^1.7.0"
    },
    "devDependencies": {
        "sass-embedded": "^1.88.0"
    }
}
